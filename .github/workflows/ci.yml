# .github/workflows/ci.yml
name: "CI / Sécurité"

on:
  push:
    branches: [ main, 'release/*' ]
  pull_request:
    branches: [ main ]

jobs:
  semgrep-check:
    name: Semgrep Security Scan
    uses: ./.github/workflows/semgrep.yml
    with:
      # Chemin vers ta config Semgrep (optionnel)
      semgrep_config: '.semgrep.yml'
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
